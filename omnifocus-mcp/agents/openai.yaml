interface:
  display_name: "OmniFocus MCP"
  short_description: "Run safe OmniFocus workflows with MCP and fuzzy candidate discovery"
  default_prompt: "Use $omnifocus-mcp to capture, organize, review, and safely mutate OmniFocus items via ID-based flows."
